/* ===================================================================
   Mujeeb Adegboye — Digital Marketing Manager & Ecommerce Specialist
   Design system: "Case File" — white ground, dashed connectors,
   monospace data marks, single coral + teal accent pair.
   =================================================================== */

@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@600;700;800&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500;700&display=swap');

:root{
  --bg:#FFFFFF;
  --bg-alt:#FAFAF9;
  --ink:#14171F;
  --muted:#666B78;
  --line:#E7E5DF;
  --coral:#FF5A36;
  --coral-soft:#FFF0EA;
  --teal:#0F8B8D;
  --teal-soft:#E9F6F6;
  --navy:#1B3A6B;
  --green:#2F9E62;
  --radius:14px;
  --max:1180px;
}

*{box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{
  margin:0;
  background:var(--bg);
  color:var(--ink);
  font-family:'Inter',sans-serif;
  font-size:16px;
  line-height:1.65;
  -webkit-font-smoothing:antialiased;
}
img{max-width:100%; display:block;}
a{color:inherit; text-decoration:none;}
ul{margin:0; padding:0; list-style:none;}

h1,h2,h3,h4{
  font-family:'Plus Jakarta Sans',sans-serif;
  font-weight:800;
  letter-spacing:-0.01em;
  line-height:1.08;
  margin:0 0 16px;
}
.mono{font-family:'JetBrains Mono', monospace;}

.wrap{max-width:var(--max); margin:0 auto; padding:0 32px;}
@media(max-width:640px){.wrap{padding:0 20px;}}

.eyebrow{
  display:inline-flex;
  align-items:center;
  gap:8px;
  font-family:'JetBrains Mono', monospace;
  font-size:12.5px;
  font-weight:700;
  letter-spacing:0.06em;
  text-transform:uppercase;
  color:var(--coral);
  margin-bottom:14px;
}
.eyebrow::before{
  content:"";
  width:8px; height:8px;
  background:var(--coral);
  border-radius:2px;
  transform:rotate(45deg);
}

/* Buttons */
.btn{
  display:inline-flex;
  align-items:center;
  gap:8px;
  font-family:'Inter',sans-serif;
  font-weight:700;
  font-size:14.5px;
  padding:13px 24px;
  border-radius:999px;
  border:2px solid transparent;
  cursor:pointer;
  transition:transform .15s ease, background .15s ease, color .15s ease;
}
.btn-primary{background:var(--coral); color:#fff;}
.btn-primary:hover{transform:translateY(-2px); background:#E64A28;}
.btn-outline{border-color:var(--ink); color:var(--ink); background:transparent;}
.btn-outline:hover{transform:translateY(-2px); background:var(--ink); color:#fff;}
.btn-sm{padding:9px 18px; font-size:13px;}

/* Nav */
.site-header{
  position:sticky; top:0; z-index:50;
  background:rgba(255,255,255,0.92);
  backdrop-filter:blur(8px);
  border-bottom:1px solid var(--line);
}
.nav{
  display:flex; align-items:center; justify-content:space-between;
  padding:14px 0;
}
.logo{
  font-family:'Plus Jakarta Sans',sans-serif;
  font-weight:800;
  font-size:18px;
  letter-spacing:-0.01em;
}
.logo span{color:var(--coral);}
.nav-links{display:flex; align-items:center; gap:30px;}
.nav-links a{font-size:14.5px; font-weight:600; color:var(--ink); position:relative;}
.nav-links a.active{color:var(--coral);}
.nav-links a:hover{color:var(--coral);}
.nav-cta{display:flex; align-items:center; gap:18px;}
.nav-toggle{display:none; background:none; border:none; font-size:24px; cursor:pointer;}

@media(max-width:900px){
  .nav-links{
    position:fixed; top:64px; left:0; right:0; bottom:0;
    background:#fff; flex-direction:column; align-items:flex-start;
    padding:32px; gap:22px; transform:translateX(100%);
    transition:transform .25s ease; overflow-y:auto;
  }
  .nav-links.open{transform:translateX(0);}
  .nav-toggle{display:block;}
  .nav-cta .btn{display:none;}
}

/* Hero */
.hero{padding:56px 0 40px; border-bottom:1px solid var(--line);}
.hero h1{font-size:52px; max-width:760px;}
.hero .lead{font-size:18px; color:var(--muted); max-width:560px; margin-bottom:28px;}
.hero-actions{display:flex; gap:14px; flex-wrap:wrap;}
@media(max-width:640px){.hero h1{font-size:34px;} .hero{padding:48px 0 40px;}}

/* Stat strip */
.stat-strip{
  display:grid; grid-template-columns:repeat(4,1fr); gap:0;
  border:1px dashed var(--line); border-radius:var(--radius);
  margin-top:28px; overflow:hidden;
}
.stat-strip .stat{
  padding:16px 18px; border-right:1px dashed var(--line);
}
.stat-strip .stat:last-child{border-right:none;}
.stat .num{font-family:'JetBrains Mono',monospace; font-weight:700; font-size:26px; color:var(--coral);}
.stat .label{font-size:12.5px; color:var(--muted); margin-top:4px;}
@media(max-width:800px){.stat-strip{grid-template-columns:1fr 1fr;} .stat-strip .stat:nth-child(2){border-right:none;}}

/* Sections */
section{padding:52px 0;}
section.alt{background:var(--bg-alt);}
.section-head{max-width:640px; margin-bottom:28px;}
.section-head h2{font-size:32px;}
.section-head p{color:var(--muted); font-size:16px;}

/* Trust strip */
.trust-row{
  display:flex; align-items:center; justify-content:space-between;
  flex-wrap:wrap; gap:24px; padding:28px 0;
  border-bottom:1px solid var(--line); border-top:1px solid var(--line);
}
.trust-row .trust-label{font-family:'JetBrains Mono',monospace; font-size:12px; color:var(--muted); text-transform:uppercase; letter-spacing:.06em;}
.trust-row .logos{display:flex; gap:32px; flex-wrap:wrap; align-items:center;}
.trust-row .logos span{font-family:'Plus Jakarta Sans',sans-serif; font-weight:700; font-size:15px; color:var(--muted); opacity:.7;}

/* Cards: ticket-stub */
.card-grid{display:grid; grid-template-columns:repeat(3,1fr); gap:14px;}
@media(max-width:900px){.card-grid{grid-template-columns:1fr 1fr;}}
@media(max-width:640px){.card-grid{grid-template-columns:1fr;}}

.ticket{
  border:1px dashed var(--line);
  border-radius:var(--radius);
  padding:20px 20px;
  background:#fff;
  display:flex; flex-direction:column; height:100%;
  transition:border-color .15s ease, transform .15s ease;
}
.ticket:hover{border-color:var(--coral); transform:translateY(-3px);}
.ticket .icon{
  width:44px; height:44px; border-radius:10px;
  background:var(--coral-soft); color:var(--coral);
  display:flex; align-items:center; justify-content:center;
  font-family:'JetBrains Mono',monospace; font-weight:700; margin-bottom:16px;
}
.ticket h3{font-size:18px; margin-bottom:8px;}
.ticket p{font-size:14.5px; color:var(--muted); margin-bottom:16px; flex-grow:1;}
.ticket .link{font-size:13.5px; font-weight:700; color:var(--coral);}

/* Process — numbered, dashed connector (real sequence) */
.process{position:relative; margin-top:20px;}
.process-row{
  display:flex; gap:20px; padding:16px 0;
  border-top:1px dashed var(--line);
}
.process-row:last-child{border-bottom:1px dashed var(--line);}
.process-num{
  font-family:'JetBrains Mono',monospace; font-weight:700; font-size:14px;
  color:var(--coral); width:56px; flex-shrink:0; padding-top:2px;
}
.process-body h4{font-size:16.5px; margin-bottom:4px;}
.process-body p{font-size:14.5px; color:var(--muted); margin:0;}

/* Feature / split */
.split{display:grid; grid-template-columns:1fr 1fr; gap:36px; align-items:center;}
@media(max-width:900px){.split{grid-template-columns:1fr; gap:32px;}}
.split img, .media-block{
  border-radius:var(--radius); border:1px solid var(--line);
  aspect-ratio:4/3; object-fit:cover; background:var(--bg-alt);
}
.media-placeholder{
  border:1px dashed var(--line); border-radius:var(--radius);
  aspect-ratio:4/3; background:var(--bg-alt);
  display:flex; align-items:center; justify-content:center;
  color:var(--muted); font-family:'JetBrains Mono',monospace; font-size:12.5px; text-align:center; padding:20px;
}

/* Result pill */
.result-pill{
  display:inline-flex; align-items:center; gap:8px;
  background:var(--teal-soft); color:var(--teal);
  font-family:'JetBrains Mono',monospace; font-weight:700; font-size:13px;
  padding:6px 14px; border-radius:999px; margin-bottom:14px;
}

/* CTA band */
.cta-band{
  background:var(--navy); color:#fff; border-radius:var(--radius);
  padding:40px 40px; text-align:center; margin:0 32px;
  max-width:calc(var(--max) - 64px); margin-inline:auto;
}
.cta-band h2{color:#fff; font-size:30px;}
.cta-band p{color:#B7BAC3; margin-bottom:26px;}
.cta-band .btn-primary{background:var(--coral);}
.cta-band .btn-outline{border-color:#fff; color:#fff;}
.cta-band .btn-outline:hover{background:#fff; color:var(--navy);}
@media(max-width:640px){.cta-band{padding:40px 24px; margin:0 20px;}}

/* Footer */
.site-footer{border-top:1px solid var(--line); padding:36px 0 22px; margin-top:24px;}
.footer-top{display:flex; justify-content:space-between; flex-wrap:wrap; gap:24px; margin-bottom:22px;}
.footer-col h5{font-family:'JetBrains Mono',monospace; font-size:12px; text-transform:uppercase; letter-spacing:.06em; color:var(--muted); margin-bottom:14px;}
.footer-col a, .footer-col li{display:block; font-size:14.5px; margin-bottom:9px; color:var(--ink);}
.footer-col a:hover{color:var(--coral);}
.footer-bottom{
  display:flex; justify-content:space-between; align-items:center;
  border-top:1px solid var(--line); padding-top:22px; flex-wrap:wrap; gap:12px;
}
.footer-bottom p{font-size:13px; color:var(--muted); margin:0;}
.social-row{display:flex; gap:16px;}
.social-row a{font-size:13px; font-weight:600; color:var(--muted);}
.social-row a:hover{color:var(--coral);}

/* Forms */
.form-block{
  border:1px solid var(--line); border-radius:var(--radius);
  padding:32px; background:var(--bg-alt);
}
.field{margin-bottom:18px;}
.field label{
  display:block; font-size:13px; font-weight:700; margin-bottom:7px;
  font-family:'JetBrains Mono',monospace; text-transform:uppercase; letter-spacing:.03em; color:var(--ink);
}
.field input, .field select, .field textarea{
  width:100%; padding:12px 14px; border:1px solid var(--line);
  border-radius:9px; font-family:'Inter',sans-serif; font-size:14.5px;
  background:#fff; color:var(--ink);
}
.field input:focus, .field select:focus, .field textarea:focus{
  outline:2px solid var(--coral); outline-offset:1px; border-color:var(--coral);
}
.field textarea{resize:vertical; min-height:110px;}
.form-note{font-size:13px; color:var(--muted); margin-top:14px;}

/* Tags */
.tag-row{display:flex; gap:8px; flex-wrap:wrap; margin-bottom:10px;}
.tag{
  font-family:'JetBrains Mono',monospace; font-size:11.5px; font-weight:700;
  background:var(--teal-soft); color:var(--teal); padding:4px 10px; border-radius:999px;
}

/* Breadcrumb */
.crumb{font-size:13px; color:var(--muted); margin-bottom:18px;}
.crumb a{color:var(--muted);}
.crumb a:hover{color:var(--coral);}

/* Principles row (About) */
.principles{display:grid; grid-template-columns:repeat(4,1fr); gap:18px;}
@media(max-width:900px){.principles{grid-template-columns:1fr 1fr;}}
@media(max-width:560px){.principles{grid-template-columns:1fr;}}
.principle{border-top:2px solid var(--coral); padding-top:14px;}
.principle h4{font-size:15.5px; margin-bottom:6px;}
.principle p{font-size:13.5px; color:var(--muted); margin:0;}

/* Blog */
.post-grid{display:grid; grid-template-columns:1fr 1fr; gap:24px;}
@media(max-width:700px){.post-grid{grid-template-columns:1fr;}}
.post-card{border:1px dashed var(--line); border-radius:var(--radius); overflow:hidden;}
.post-card .thumb{aspect-ratio:16/9; background:var(--bg-alt); border-bottom:1px dashed var(--line);}
.post-card .body{padding:22px;}
.post-card .date{font-family:'JetBrains Mono',monospace; font-size:12px; color:var(--muted); margin-bottom:8px;}
.post-card h3{font-size:18px; margin-bottom:8px;}
.post-card p{font-size:14px; color:var(--muted);}

article.post-body{max-width:720px; margin:0 auto;}
article.post-body h2{font-size:24px; margin-top:36px;}
article.post-body p{font-size:16px; color:var(--ink);}
article.post-body ul{margin:16px 0; padding-left:20px; list-style:disc;}
article.post-body li{margin-bottom:8px; font-size:15.5px;}

/* Gated download modal */
.dg-overlay{
  position:fixed; inset:0; background:rgba(20,23,31,0.55);
  display:none; align-items:center; justify-content:center; z-index:200;
  padding:20px;
}
.dg-overlay.open{display:flex;}
.dg-modal{
  background:#fff; border-radius:16px; max-width:420px; width:100%;
  padding:32px 28px; position:relative; box-shadow:0 20px 60px rgba(0,0,0,0.2);
}
.dg-close{
  position:absolute; top:14px; right:16px; background:none; border:none;
  font-size:20px; cursor:pointer; color:var(--muted); line-height:1;
}
.dg-modal .eyebrow{margin-bottom:10px;}
.dg-title{font-size:19px; margin-bottom:6px;}
.dg-sub{font-size:13.5px; color:var(--muted); margin-bottom:18px;}
.dg-status{font-size:13px; margin-top:10px; min-height:16px;}
